# TODO.md

- Standardize the names of variables, global variables, CMake objects, preprocessor argument variables, and so on.
- Review and rewrite the part of the code related to the GUI, as it was rather poorly copied from another project to create a minimal window with text validation. It is also necessary to comment out the code and remove references to "requiem," as these are references to another project.